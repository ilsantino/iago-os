# `_config/runbooks/`

Repeatable operational how-tos, one per file: `{slug}.md`. A runbook gets written
the second time a procedure is carried out by hand.

L3 — configured once, edited rarely, read by agents as a constraint rather than
as input.
