# `summaries/`

`{plan-slug}.md`, one per executed plan, written by the pipeline's summary stage.

Nothing else lives here. Dispatch logs, PR-body drafts and review dumps are
per-run artefacts and belong in `state/`.
