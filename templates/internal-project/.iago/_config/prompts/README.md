# `_config/prompts/`

Reusable prompt fragments, one per file: `{use-case}.md`. A fragment earns a file
the moment a second dispatch would otherwise copy it.

Per-run substituted prompts are regenerable from these and belong in `state/`.

L3 — configured once, edited rarely, read as a constraint.
