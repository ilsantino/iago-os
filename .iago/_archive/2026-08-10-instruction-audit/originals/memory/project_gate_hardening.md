---
name: project_gate_hardening
description: feature-gate-hardening batch (PR
metadata: 
  node_type: memory
  type: project
  originSessionId: 0519768b-a1a4-40f9-a9cb-2a84b8a35e25
---

Gate-hardening batch implemented 2026-06-16 on branch `fix/gate-hardening` → **PR #96** (https://github.com/ilsantino/iago-os/pull/96), base main. Review-infra only (the pipeline's own workflow scripts; no TS/React/Amplify build). Implemented DIRECTLY (not via /iago-execute) per the agreed gate: dual-adversarial-fix → /iago-prfix. Plans at `.iago/plans/feature-gate-hardening/01-04.md` (committed in the PR).

Files: `.claude/workflows/{classify-tier.mjs, classifyTier.test.mjs, execute-pipeline.js, execute-pipeline.test.mjs, dual-adversarial.js, dual-adversarial.test.mjs}`, `.github/workflows/validate.yml`, `.iago/research/2026-05-30-pipeline-dynamic-upgrade.md`.

Key invariants / gotchas hit (verify still true before editing these files):
- **classify-tier.mjs has a BYTE-IDENTICAL inline twin in execute-pipeline.js**, guarded by `classifyTier.test.mjs` drift test (now anchored to a `// END classifyTier` sentinel in BOTH copies). Edit both in lockstep; the guard normalizes whitespace + strips comments, so only LOGIC must match.
- **`node --check` FAILS on execute-pipeline.js / dual-adversarial.js** (top-level `return` — they are workflow *bodies*). Use `node scripts/validate-workflows.mjs` to compile-check them; the harnesses execute them. Only the `.mjs` files pass `node --check`.
- **biome (format hook) can't parse those two `.js` files** (illegal-return) → it's a no-op on them; but it DID flip a fresh `let`→`const` (useConst) when declaration + reassignment landed in separate edits — use `const x = [...]` + `x.push(...)` to avoid reassignment entirely. See [[feedback_format_hook_let_const_flip]].
- Harness counts after this batch: classifyTier 22, execute-pipeline 23, dual-adversarial 44.

Tiering corrections baked in: `tier_override` clamped to [1,3] (0/out-of-range ignored so it can't void fail-safes); missing-EOF-sentinel → Tier 3, headings-missing → Tier 2 (kept). Compliance read-only guard snapshots HEAD **and** `git status --porcelain` (dirty-tree with unchanged HEAD must fail).

Status as of 2026-06-16 (mid fix-cycle-1):
- Commit `cf687d2` = initial 4-plan implementation (pushed, PR #96 open vs main).
- Dual-adversarial Team gate ran (pre-tag): clean=false, gateStatus=COMPLETE, filtered=[] (skeptics dropped nothing). 1 Critical + 4 Important verified-KEPT. Fixed DIRECTLY (not via fix-Workflow — self-modifying workflow edits + byte-identical twin).
- Fix-cycle-1 edits MADE but **UNCOMMITTED** (working tree: dual-adversarial.js + .test.mjs, execute-pipeline.js + .test.mjs, research doc). Resolutions:
  1. tier_override regex → frontmatter-fence-scoped + `(\d+)` reject-out-of-range (closes Critical: `10`→`1` silent down-tier; closes prose-injection Minor).
  2. Fail-safes (sentinel + headings) NO LONGER suppressed by any override (removed `tierOverride===undefined` guard) — truncated read escalates regardless.
  3. dual-adversarial: a failed AUTO-DERIVED load-bearing lens (security/amplify/frontend), scoped to `!probeDegraded` (precise probe only), → gateStatus INCOMPLETE.
  4. Added orchestration-level tier_override tests (10 rejected / 1 lowers / truncated→Tier3 / prose-ignored) + allFiltered multi-round accumulation test + lens-INCOMPLETE precise/base/degraded tests.
- Post-fix harnesses GREEN: classifyTier 22, execute-pipeline 28, dual-adversarial 47, validate-workflows OK.
- fix-cycle-1 COMMITTED as `2d2ac32` (pushed). Re-gate run #2 came back gateStatus=INCOMPLETE but ONLY due to transient platform RATE-LIMITING ("Server is temporarily limiting requests" — codex/review/team:data/team:arch/codeQuality legs failed to RUN), NOT a code verdict. It surfaced 1 legit Important from the completeness leg: my `!probeDegraded` scope on lensIncomplete created a producer/consumer gap (degraded probe + a failed speculative load-bearing lens → ships clean because probeDegraded is only a non-blocking caveat the consumer ignores).
- fix-cycle-2 COMMITTED as `a9355cf` (pushed): DROPPED `!probeDegraded` — a failed auto-derived load-bearing lens (security/amplify/frontend) now ALWAYS forces INCOMPLETE (precise OR speculative). Inverted the degraded-probe test. dual-adversarial 47/47 green; execute-pipeline 28, classifyTier 22 (untouched this cycle).
- Re-gate run #3 (2026-06-16, rate-limit CLEARED): gateStatus=COMPLETE, all 11 legs ran (incompleteLegs=[]), Opus leg verdict PASS, codexSource=codex (no fallback). clean=false, blocking=1. Harnesses green pre-gate (classifyTier 22, execute-pipeline 28, dual-adversarial 47, validate-workflows OK). Findings:
  - **1 BLOCKING Important** (by codex) — execute-pipeline.js:980,1046: production /iago-execute Tier 2/3 delegation passes `const reviewLenses = []` → `lenses: reviewLenses` into runDualAdversarial; dual-adversarial.js treats explicit `[]` as the EXPLICIT path (lensSource='explicit'), so the auto-derived load-bearing lenses (security/amplify/frontend) AND this PR's new lensIncomplete-on-failed-load-bearing-lens logic (gated on `lensSource==='auto'`) NEVER fire from the production pipeline. Codex VERIFIED via blame that `reviewLenses=[]` is PRE-EXISTING (PR #89 a5900b5, already on main) — NOT a regression from this branch; gate-hardening backlog (plans 01-04) has no wiring item → coverage gap, out of THIS PR's scope. Team gate still runs Opus∥Codex∥team:data∥team:arch∥skeptic∥plan-compliance, so sensitive diffs are not unreviewed — only the path-derived specialized lens is missing. Fix (future): omit `lenses` (or pass 'auto') for default Tier 2/3 delegation, reserve `[]` for explicit opt-out, add regression test.
  - **3 Minors, all already-known/accepted residuals**: (a) broadened TIER3_KEYWORDS bare-substring over-tiering (role/sql/secret/auth etc., no word boundary) — accepted fail-safe over-review; (b)+(c) compliance read-only guard haiku sha-transcription jitter on unchanged HEAD could false-throw (two snapshots, fail-closed direction) — same residual surfaced by both team:arch and lens:completeness.
- RESOLVED 2026-06-16: Santiago MERGED #96 (squash → main `3bb7f68`) and chose Path A (follow-up PR for the wiring gap). #96 done.
- FOLLOW-UP (Path A) — branch `fix/pipeline-auto-lenses` off main, commit `7066615`: wires production /iago-execute Tier 2/3 delegation onto the dual-adversarial AUTO lens path. Edits ONLY in execute-pipeline.js: `reviewLenses []→'auto'` (L~980), `runDualAdversarial` lenses default `[]→'auto'` (L~632), JSDoc updated. dual-adversarial.js UNTOUCHED (AUTO handling already correct + 47 tests) → classifyTier byte-identical twin untouched (no drift risk). Regression test in execute-pipeline.test.mjs: asserts team delegation forwards `wargs.lenses==='auto'` (AUTO trigger) + strengthened the Tier2/Tier3 multi-delegation loops; RED→GREEN PROVEN (3 fail when reverted to []). Plan doc `.iago/plans/quick-260616-pipeline-auto-lenses.md`. STATE.md: #96 merge row + Updated 2026-06-16 + GH-15 note. Harnesses GREEN: classifyTier 22, execute-pipeline 29 (+1), dual-adversarial 47, validate-workflows OK.
- DONE 2026-06-16: Follow-up shipped as **PR #97** (https://github.com/ilsantino/iago-os/pull/97), base main, branch fix/pipeline-auto-lenses HEAD `7066615`. Flow was gate-before-tag on the local branch (gate is read-only, diffs base...HEAD; prNumber optional).
  - dual-adversarial Team gate run #1 ABORTED on a SIDE-EFFECT BREACH false-positive = **GH-15 haiku sha-transcription jitter** ACTUALLY FIRING: the gate's own read-only HEAD-snapshot guard took two haiku snapshots of the unchanged HEAD; one dropped a digit (`7066152c…` vs real `70666152c…`), porcelain identical empty. Verified ground truth (git rev-parse HEAD == 70666152c…, tree clean, 1 ahead of main) → NOT a real mutation, NOT a verdict. Treated like a rate-limit INCOMPLETE → RE-RAN (does not count as a fix cycle). Lesson: GH-15 jitter is real and bites the gate's own guard, not just execute-pipeline's compliance guard — a candidate for a future deterministic-snapshot fix (out of scope here).
  - Gate run #2: COMPLETE + clean, verdict PASS, 0 blocking, codexSource=codex (genuine cross-model, not degraded). 5 Minor findings, ALL no-action/intended: stray sed temp file (a review leg's, already gone — tree verified clean), a one-off 26/3 test result the reviewer couldn't reproduce (= my deliberate RED-state proof run; 29/0 otherwise), `lenses='auto'` destructure default is dead from the current call graph (intended defensive default), STATE row documents #96 not #97 (correct — #97's row lands when it merges), AUTO path = more fail-closed re-runs on infra flakiness (intended posture).
  - Single @claude tag posted via /iago-prfix (context-rich; issuecomment-4723984151). Async review-fix loop running on GH Actions. NEVER merged — Santiago merges. After the async loop reports clean it's safe to merge (the cross-model Team gate already passed clean on this exact diff `7066615`).
  - Caveat for continuation: if the async @claude loop PUSHES fix commits, HEAD moves off `7066615` → re-sync + re-gate before trusting clean (see [[feedback_async_claude_loop_stale_ref]], [[feedback_sync_before_pr_fix]]).
- Residual Minors (NOT fixed, reported only): haiku porcelain-snapshot non-determinism (fail-safe), broadened-keyword over-tiering cost, biome style debt on the 2 .js files, eofSeen backward-incompat (by design).
See [[feedback_dual_adversarial_fix_before_claude_tag]], [[project_pipeline_v2]], [[feedback_format_hook_let_const_flip]].
