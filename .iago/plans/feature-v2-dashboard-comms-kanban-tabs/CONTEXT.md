# `.iago/plans/feature-v2-dashboard-comms-kanban-tabs/` — v2 Dashboard Comms + Kanban + Workflows Tabs (MWP L2 Stage Contract)

**Stage:** iago-os v2 Phase 6 (increment) — three read-only dashboard tabs over existing file-bus + NDJSON state. NOT a new phase; rides the Phase 6 Next.js dashboard build.
**Layer:** L2 — stage contract.
**Token budget:** 200–500 tokens of contract content.
**Sibling stage:** the main Phase 6 dashboard stack (agent list across 5 shapes + cost + intervention controls). This folder scopes ONLY the comms/board/workflows tabs.
**Preceding stage:** `feature-v2-agent-comms-channel/CONTEXT.md` (the Comms tab needs comms/<date>.ndjson to exist) + `feature-v2-phase-1-daemon` (file-bus task dirs + ipc-server shipped).

This stage produces 1–2 plan files. The cortextOS 'comms tab' is backed by the real comms channel; the 'workflows tab' maps to cron registrations; the kanban (Board) renders the file-bus lifecycle. The literal cortextOS 'business analytics software' and 'email outreach dashboards' are NOT ported — out of v2 scope.

## Inputs

### Layer 3 (reference — internalized as constraints, NOT processed as input)
- `docs/specs/iago-os-v2-vision.md` § Phase Sequencing Phase 6 (2026-05-29 dashboard-tabs amendment)
- `docs/specs/iago-os-v2-master-prompt.md` § P1 item 8 (full Next.js dashboard, 2026-05-29 tabs amendment)
- `.iago/research/2026-05-29-cortextos-comms-gap-analysis.md`
- `runtime/daemon/ipc-server.ts` (IpcRequest surface to extend with list-tasks + list-crons; honor H1 buffer + H2 idle caps)
- `runtime/daemon/file-bus.ts` (the three task dirs to read) + `comms/<date>.ndjson` (from feature-v2-agent-comms-channel) + crons.json
- `.claude/rules/react-vite.md` (React 19 + Vite + TS + Tailwind + ShadCN) + `.claude/rules/e2e-testing.md` (Playwright) + `.claude/rules/tdd.md`
- `CLAUDE.md` (L0) + `.claude/rules/execution-pipeline.md`

### Layer 4 (product — processed as input each run)
- *Plan files (`NN-*.md`)* — generated by `/iago-plan --feature`, consumed by `/iago-execute feature-v2-dashboard-comms-kanban-tabs`

## Process
1. **Plan** — `/iago-plan --feature "Phase 6 dashboard tabs: Comms (tail comms/<date>.ndjson over IPC, threaded agent-to-agent view), Board (3-column kanban over tasks/{pending,claimed,resolved}/, In-Progress cards show owner agentId + shape badge), Workflows (cron registrations + last-fire telemetry); add list-tasks + list-crons IPC methods honoring H1/H2 caps"` with all L3 loaded.
2. **Stress test** — surface: IPC payload caps under many tasks, NDJSON tail pagination, stale-binding on board cards.
3. **Approval gate** — present to Santiago (folds into Phase 6 go-ahead).
4. **Execute** — `/iago-execute feature-v2-dashboard-comms-kanban-tabs`. Full pipeline.
5. **Verify** — Comms tab renders live NDJSON; Board shows tasks moving columns; Workflows shows crons + last-fire; Framer Motion on transitions (per memory feedback_framer_motion); ShadCN verified against official Vite docs (per memory feedback_shadcn); ≥80% coverage; Playwright e2e for the board; self-evidence screenshot of live tasks moving columns.
6. **Session digest** — Obsidian; update STATE.md.

## Outputs
| Output | Location | Status |
|---|---|---|
| Dashboard-tabs plan stack | `.iago/plans/feature-v2-dashboard-comms-kanban-tabs/NN-*.md` | Pending |
| `list-tasks` + `list-crons` IPC methods | `runtime/daemon/ipc-server.ts` | Pending |
| Comms / Board / Workflows tab components | `apps/dashboard/src/features/*` | Pending |
| Tests (Vitest + Playwright) + README | colocated + `e2e/` | Pending |
| PR(s) @claude tagged | GitHub | Pending |

## Decided constraints (do not relitigate during planning)
- **Read-only views** — no new data model; render existing file-bus + NDJSON state.
- **Status is the directory** — kanban columns map pending=To-Do, claimed=In-Progress, resolved=Done.
- **SKIP nothing — Comms tab has real data** because feature-v2-agent-comms-channel ships first.
- **Do NOT port** cortextOS business-analytics or email-outreach dashboards (out of v2 scope).
- **React 19 + Vite + TS strict + Tailwind 4 + ShadCN (verify vs official docs) + Framer Motion** per stack rules.
- **Honor H1 buffer + H2 idle caps** on IPC endpoints.
- **No Postgres, no Docker. Pipeline preserved.**
- **Garry-impressed completeness.**

## Open questions to capture (not block)
- Community-PRs view (low priority; defer unless Santiago wants it).
- Per-agent filter on the Comms tab.

## Source
- L0: `CLAUDE.md` · L1: `.iago/CONTEXT.md` · Sibling L2: main Phase 6 dashboard stack · Decision artifact: `.iago/research/2026-05-29-cortextos-comms-gap-analysis.md`