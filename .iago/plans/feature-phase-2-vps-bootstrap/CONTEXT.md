# `.iago/plans/feature-phase-2-vps-bootstrap/` — v2 Phase 2 VPS Bootstrap (MWP L2 Stage Contract)

**Stage:** iago-os v2 Phase 2 — daemon install on Hostinger VPS via FAST single-hour cutover; OpenClaw archive (encrypted, 30-day retention); Telegram bot rotation via BotFather `/revoke`; Anthropic 3-profile credential provisioning; LanceDB drop (MemPalace canonical); WhatsApp deauth via Meta Graph API; first-real-workflow = PR-triage agent.
**Layer:** L2 — stage contract.
**Token budget:** 200–500 tokens of contract content.
**Sibling stage:** `runtime/CONTEXT.md` (broader v2 daemon build umbrella covering Phase 0 → Phase 12).
**Preceding stage:** `.iago/plans/feature-v2-phase-1-daemon/CONTEXT.md` (Phase 1 — daemon skeleton local, acceptance gate landed in commit `4ee40ee`).

This stage produces 5 numbered plan files (`01-*.md` … `05-*.md`) that, when executed through the iaGO pipeline, ship Phase 2 of v2: the daemon running as a system-level systemd unit on the Hostinger VPS (srv1456441 / 187.77.135.32), with Telegram control surface rotated to a fresh BotFather token, OpenClaw stopped + archived (age-encrypted, 30-day retention via systemd timer), Anthropic 3-profile credentials provisioned via systemd `LoadCredentialEncrypted=`, WhatsApp deauthed at Meta, and a cron-fired PR-triage agent producing a daily Telegram summary. Acceptance gate is the 8-criterion checklist in § 10 of the Phase 2 delivery spec (`.iago/research/2026-05-16-phase-2-vps-bootstrap-spec.md`).

## Inputs

### Layer 3 (reference — internalized as constraints, NOT processed as input)

- `docs/specs/iago-os-v2-master-prompt.md` § Acceptance criteria + § Phased sequencing (Phase 2 row); Garry-impressed completeness standard
- `docs/specs/iago-os-v2-vision.md` § Phase Sequencing (Phase 2 originally "alongside OpenClaw" framing — **SUPERSEDED** by FAST cutover per Santiago override 2026-05-16)
- `.iago/decisions/2026-05-15-agent-shape-taxonomy.md` § HTTP-shape adapter authentication (systemd `LoadCredential=` verdict — drives credential design)
- `.iago/research/2026-05-13-multi-agent-cohabitation.md` — cortextOS + Hermes + Paperclip primitives (PR-triage uses Hermes wake-check pattern)
- `CLAUDE.md` (repo root, L0) — execution discipline + pipeline rules + memory architecture + tech stack constraints
- `.claude/rules/tdd.md` — RED-GREEN-REFACTOR; 80% coverage floor on new TS code
- `.claude/rules/execution-pipeline.md` — mandatory pipeline stages 0–6
- `.claude/rules/layer-triage.md` — 60/30/10 framework (Phase 2 is mostly deterministic + rule-based; AI surface is only the PR-triage agent prompt)
- `.claude/rules/systematic-debugging.md` — 4-phase debugging discipline for any blocked plan
- `.claude/rules/mcp-server-patterns.md` — load if any plan introduces an MCP server (none expected Phase 2)

### Layer 4 (product — processed as input each run)

- `.iago/research/2026-05-16-phase-2-vps-bootstrap-spec.md` — **PRIMARY SOURCE**; 10 sections + open questions + references. Every plan derives content from this file. Each command in the runbooks must land verbatim (or with explicit reasoning if altered) inside a plan task.
- `.iago/research/2026-05-16-v2-operational-migration-scope.md` § 1 (first-real-workflow = PR-triage agent) + § 2 (Anthropic auth migration — Phase 2 provisions, Phase 3 wires) + § 3 (memory layer cutover — LanceDB drop)
- `runtime/migration/00-vps-audit.md` — Phase 0 audit (OpenClaw inventory: user systemd unit `openclaw-gateway.service` under `user@1001.service` for `ilsantino`; `~/.openclaw/` ~12 MiB credentials + 72 KiB LanceDB; no crontab; age + tar + jq present; Debian 13)
- `runtime/migration/phase-1-rollback.md` — Phase 1 rollback shape (reference pattern for Phase 2 rollback structure)
- `runtime/PHASE-1-EVIDENCE.md` — Phase 1 self-evidence template (Phase 2 evidence template extends this pattern)
- `runtime/daemon/config.ts` — DaemonConfig + AgentConfig schema (extended in Plan 01 with optional `authProfile` field per spec § 5)
- `runtime/daemon/main.ts` — startDaemon flow (insertion point for `loadSystemdCredentials()` per spec § 1 credential bootstrap helper)
- `runtime/daemon/state-paths.ts` — IAGO_DAEMON_STATE_ROOT resolution (systemd unit sets to `/var/lib/iago-os/daemon-state`)
- *Plan files written into this folder (`NN-*.md`)* — generated by this `/iago-plan --feature` invocation, consumed by `/iago-execute feature-phase-2-vps-bootstrap`

## Process

1. **Plan** — already executed: this run produced 5 plan files (`01-*.md` … `05-*.md`) derived from the Phase 2 delivery spec. Each plan ≤8 tasks (per `.iago/config.json` `planning.max_tasks_per_plan`).
2. **Stress test** — every plan stress-tested inline (`## Stress Test` section appended). Pipeline step 0 skips re-stress on plans that already carry that section.
3. **Approval gate** — present the plan stack to Santiago. Do NOT skip. Do NOT proceed to execute without explicit "go". The cutover runbook (Plan 03) executes against PRODUCTION VPS state — approval is non-negotiable.
4. **Execute** — `/iago-execute feature-phase-2-vps-bootstrap`. Full 8-stage pipeline per plan. Wave grouping: wave 1 = plans 01 + 02 in parallel (no dependency between credential infra + OpenClaw teardown scripts); wave 2 = plans 03 + 04 in parallel (cutover runbook + PR-triage agent — both depend on wave 1); wave 3 = plan 05 (verification + acceptance gate — depends on 03 + 04).
5. **Verify** — Phase 2 acceptance criteria 1–8 per spec § 10. Self-evidence (criterion #8) means real terminal log of the cutover + rollback + Telegram screenshot from phone embedded in PR description. NOTE: no staging VPS per Santiago override (spec § 10 criterion #3 proposed $9/mo staging KVM, Santiago skipped); cutover dry-run is executed locally via `runtime/scripts/test-cutover.mjs` (following the `test-rollback.mjs` pattern) before the real cutover window.
6. **Cutover execution** — Plan 03 runbook executed by Santiago at the scheduled window (default Sunday 8pm US/Mexico per spec open question #1). Not part of the pipeline itself; pipeline ships the scripts + docs, Santiago triggers the cutover with the scripts present and tested.
7. **Session digest** — write `sessions/2026-MM-DD-iago-os-v2-phase-2.md` to the Obsidian vault per `~/.claude/rules/obsidian.md`. Update STATE.md per project rule.

## Outputs

| Output | Location | Status |
|---|---|---|
| Phase 2 plan stack (5 plans, ~22 tasks, 3 waves) | `.iago/plans/feature-phase-2-vps-bootstrap/NN-*.md` | Pending — generated by this `/iago-plan` run |
| systemd unit file | `runtime/deploy/iago-os-v2-daemon.service` | Pending — Plan 01 |
| Credential provisioning script | `runtime/deploy/provision-credentials.sh` | Pending — Plan 01 |
| Credential bootstrap helper | `runtime/daemon/cred-bootstrap.ts` + `.test.ts` | Pending — Plan 01 |
| AgentConfig schema extension (`authProfile` field) | `runtime/daemon/config.ts` (extend) | Pending — Plan 01 |
| OpenClaw archive script + retention timer | `runtime/deploy/archive-openclaw.sh` + `iago-archive-prune.{service,timer}` (generated by script) | Pending — Plan 02 |
| WhatsApp deauth runbook | `runtime/deploy/revoke-whatsapp.sh` + `runtime/migration/02-whatsapp-deauth.md` | Pending — Plan 02 |
| Telegram bot rotation procedure | `runtime/deploy/rotate-telegram-bot.sh` + `runtime/migration/02-telegram-bot-rotation.md` | Pending — Plan 02 |
| Cutover runbook (executable + human-readable) | `runtime/deploy/cutover.sh` + `runtime/migration/02-cutover-runbook.md` | Pending — Plan 03 |
| Rollback runbook (executable + human-readable, ≤4min target) | `runtime/deploy/rollback.sh` + `runtime/migration/02-rollback-runbook.md` | Pending — Plan 03 |
| Cutover dry-run script | `runtime/scripts/test-cutover.mjs` | Pending — Plan 03 |
| Cutover decisions log (LanceDB drop / User=iago / Telegram Option A) | `runtime/migration/02-cutover-decisions.md` | Pending — Plan 03 |
| PR-triage agent config + prompt + cron entry | `runtime/agents/pr-triage/agent-config.json` + `prompt-template.md` + `README.md` | Pending — Plan 04 |
| PR-triage integration test | `runtime/agents/pr-triage/pr-triage.test.ts` | Pending — Plan 04 |
| Phase 2 evidence template | `runtime/PHASE-2-EVIDENCE.md` | Pending — Plan 05 |
| Phase 2 integration test (Tailscale SSH to VPS) | `runtime/integration/phase-2-vps.test.ts` | Pending — Plan 05 |
| Evidence-check script extension | `runtime/scripts/check-evidence.mjs` (extend) | Pending — Plan 05 |
| Pipeline summaries (one per plan) | `.iago/summaries/feature-phase-2-vps-bootstrap-NN-<timestamp>.md` | Pending — written by pipeline step 6 |
| PR(s) with @claude tagged for async review | GitHub | Pending — written by pipeline step 5b |

## Decided constraints (do not relitigate during planning)

These bindings are locked by Santiago override 2026-05-16, the Phase 2 delivery spec, and existing memory entries. Any plan contradicting them is wrong.

- **FAST cutover** — NO "alongside OpenClaw" parallel-run period. Stop OpenClaw + install v2 same hour. Rollback path = restart OpenClaw from archive + re-rotate Telegram token.
- **Same Telegram bot via BotFather `/revoke`** — preserves `@handle` + chat IDs + allowed-user-ID semantics. Per-bot rotation rejected (spec § 3 Option A verdict).
- **`User=iago` dedicated systemd system user** — NOT ilsantino, NOT root. Provisioned via `useradd --system --no-create-home --shell /usr/sbin/nologin`. Keeps `ProtectHome=true` meaningful (spec § 1 verdict).
- **systemd `LoadCredentialEncrypted=`** for all secrets (Telegram + 3 Anthropic profiles + future webhook secrets). 1Password CLI provisioning-only; plaintext NEVER touches local or remote disk. Bridge to `process.env` via `runtime/daemon/cred-bootstrap.ts` before `loadConfig()` (spec § 1 + § 2).
- **LanceDB drops at cutover** — encrypted OpenClaw archive preserves the 72 KiB content for 30 days; MemPalace is canonical going forward (spec § 6 verdict). No active fact-extraction.
- **WhatsApp deauth at cutover** — Meta Graph API `DELETE /<WABA_ID>/subscribed_apps` + `DELETE /me/permissions` per spec § 7. NOT scripted-and-tested in CI (Meta-side creds + one-time op); lives as runbook + thin wrapper script that takes the IDs as args.
- **First-real-workflow Phase 2 = PR-triage agent** (Shape 1 PTY adapter; daily cron at 14:00 UTC = 09:00 EST; queries `gh pr list` across iago-os org repos; classifies + posts single Telegram summary). NOT Sentria port, NOT full pipeline-on-VPS migration (spec extension via migration-scope § 1).
- **No staging VPS** — Santiago skipped per FAST preference. Cutover dry-run via `runtime/scripts/test-cutover.mjs` (local Vitest harness with all systemd/tailscale/age commands stubbed), NOT a separate Hostinger KVM.
- **Anthropic profiles: PROVISION at Phase 2, ACTIVATE at Phase 3** — Plan 01 adds `authProfile?: "default" | "ilsantino" | "iaguito"` field to `AgentConfig` + provisions all 3 credentials. Adapter wiring (passing the right env var to spawned process) is Phase 3 work. Phase 2 hello-world + PR-triage use the `default` profile only.
- **age-encrypted OpenClaw archive** — pubkey at `/etc/iago-os/santiago-age.pub`; private key stays on Santiago's Windows box; 30-day retention via systemd timer (NOT cron — VPS has no crontab per audit).
- **Telemetry continues** — Phase 1 NDJSON event emission extends with `daemon-start.runUnder: "systemd"` field + new `cred-bootstrap-loaded` event kind. Cutover scripts ALSO write one-line NDJSON to `/var/log/iago-os/cutover.ndjson` per major step.
- **No new MCP servers in Phase 2** — out of scope.
- **No Docker.** systemd on VPS directly. Match Phase 1 build artifacts (Node.js 20, ESM).
- **No Postgres.** Phase 2 does NOT wire SQLite cost ledger (that lands Phase 8).
- **Pipeline preserved.** `scripts/execute-pipeline.sh` runs unchanged; pipeline runs from Santiago's Windows box, not from the VPS (pipeline-on-VPS migration is Phase 11 per migration-scope § 11).
- **Garry-impressed completeness standard.** Every script idempotent; every command verified; every rollback step time-estimated; zero TODOs without tracked issue + date.

## Open questions to capture (not block) during planning

Per spec § Open questions, these are flagged for Santiago to confirm at cutover window. Defaults apply unless flagged.

- **OQ1 — Cutover window timing.** Default: Sunday 8pm US/Mexico time.
- **OQ2 — Staging VPS.** Default per spec: provision $9/mo. **Santiago override:** NO — local dry-run via `runtime/scripts/test-cutover.mjs` (Plan 03 Task 6).
- **OQ3 — `User=iago` system user.** Default: YES (locked per spec § 1 verdict).
- **OQ4 — age pubkey deployment path.** Confirmed: `/etc/iago-os/santiago-age.pub`; Santiago's private at `~/.age/santiago.key` on Windows box.
- **OQ5 — WhatsApp APP_SECRET location.** Default: 1Password vault `iago-os` item `whatsapp-app-credentials` field `app_secret`. If not present, Santiago retrieves from Meta App Dashboard at cutover-time.
- **OQ6 — Telegram bot @handle preservation.** Confirmed: YES (Option A locked).
- **OQ7 — LanceDB drop.** Confirmed: YES (locked per spec § 6 verdict).
- **OQ8 — Sebas notification.** Default: Telegram message at T-15 (before cutover starts).
- **OQ9 — Anthropic credential idle period.** Confirmed: YES — provision Phase 2, activate Phase 3.
- **OQ10 — Iaguito short-form profile name.** Confirmed: YES — `iaguito` in v2 schema vs `iaguito_anthropic_sutoken` in OpenClaw.

## Source

- L0: `CLAUDE.md` (repo root)
- L1: `.iago/CONTEXT.md`
- Sibling L2: `runtime/CONTEXT.md` (broader v2 daemon build umbrella)
- Preceding L2: `.iago/plans/feature-v2-phase-1-daemon/CONTEXT.md` (Phase 1 — daemon skeleton local; CLOSED 2026-05-15)
- This file: L2 stage contract scoped to Phase 2 only (VPS bootstrap + FAST cutover)
- Primary source spec (L4): `.iago/research/2026-05-16-phase-2-vps-bootstrap-spec.md`
- MWP rationale: `.iago/research/2026-05-13-mwp-source-synthesis.md` §3.2 (stage contracts)
