---
phase: feature-phase-2-vps-bootstrap
plan: 03
wave: 2
depends_on: [01, 02]
context: .iago/plans/feature-phase-2-vps-bootstrap/CONTEXT.md
created: 2026-05-17
source: feature
---

# Plan: feature-phase-2-vps-bootstrap/03-cutover-rollback-orchestration

## Goal

Orchestrate the two runbooks that turn the deploy artifacts (Plan 01) + teardown scripts (Plan 02) into a real cutover Santiago can execute in 60 minutes with a ≤4-minute rollback. Four deliverables: (1) `cutover.sh` — executable wrapping the spec § 8 T-15 → T+60 sequence with explicit verify commands at each checkpoint and rollback-trigger conditions; (2) `rollback.sh` — executable wrapping the spec § 9 ≤4-minute rollback; (3) `02-cutover-runbook.md` + `02-rollback-runbook.md` + `02-cutover-decisions.md` — the human-readable runbooks (operator follows these; scripts automate the deterministic parts); (4) `runtime/scripts/test-cutover.mjs` — local Vitest harness that exercises both scripts against fully-stubbed systemd/tailscale/age/curl (replaces the rejected staging-VPS option per Santiago override). Source of truth: `.iago/research/2026-05-16-phase-2-vps-bootstrap-spec.md` §§ 6, 8, 9.

## Files

| Action | Path | Purpose |
|--------|------|---------|
| create | `runtime/deploy/cutover.sh` | Executable: invokes Plan 01 + 02 scripts in the T-15 → T+60 sequence with verify at each step |
| create | `runtime/deploy/rollback.sh` | Executable: ≤4-min rollback sequence (stop v2, restore OpenClaw, re-rotate Telegram) |
| create | `runtime/migration/02-cutover-runbook.md` | Human-readable runbook (copy of spec § 8) |
| create | `runtime/migration/02-rollback-runbook.md` | Human-readable runbook (copy of spec § 9) |
| create | `runtime/migration/02-cutover-decisions.md` | Decision log: LanceDB drop / User=iago / Telegram Option A (spec §§ 1, 3, 6 verdicts) |
| create | `runtime/scripts/test-cutover.mjs` | Vitest harness simulating full cutover + rollback with stubs (no real VPS) |
| edit | `runtime/scripts/check-evidence.mjs` (only if exists; otherwise skip — Plan 05 owns extension) | (n/a Plan 03; deferred to Plan 05) |

## Tasks

### Task 1: Author cutover.sh

- **files:** `runtime/deploy/cutover.sh`
- **action:** Executable bash script that wraps spec § 8 cutover sequence. Shebang + `set -euo pipefail`. Header comments naming purpose + warning (PRODUCTION operation, requires Santiago at keyboard + explicit `IAGO_CUTOVER_CONFIRM=YES` env var to proceed) + expected wall clock (60 min) + rollback trigger list. **Idempotency contract** (pre-merge adversarial review I4 fix): every step's pre-condition is checked before the step runs (e.g., before `systemctl enable --now iago-os-v2-daemon.service`, check `tailscale ssh root@$VPS_HOST -- 'systemctl is-enabled iago-os-v2-daemon.service' 2>/dev/null` — if "enabled" AND `is-active` returns "active", skip the systemctl call with an "already enabled+active" log line). Same posture for: directory creation (`test -d` before `mkdir -p`); systemd-creds upload (compare encrypted file sha256 before re-encrypting); unit-file copy (compare sha256 before `cat > /etc/systemd/system/iago-os-v2-daemon.service`); journal/IPC checks (passive — read-only). This guarantees `cutover.sh` resumed via `IAGO_CUTOVER_RESUME_FROM=Tnn` doesn't trigger partial-state failures on already-completed steps when a Tailscale node disconnects mid-step and the operator re-runs from the same checkpoint. Constants: `VPS_HOST="${VPS_HOST:-srv1456441}"`, `VPS_USER="${VPS_USER:-root}"`, `DEPLOY_DIR="$(dirname "$0")"`, `MIGRATION_DIR="$(dirname "$0")/../migration"`, `SANTIAGO_USER_ID="${IAGO_TELEGRAM_USER_ID:-}"`. Pre-flight gate (verbatim from spec § 8 pre-cutover gate): 12 checks, each its own line — abort with the exact unchecked item if any fails. Examples: `[[ -n "$SANTIAGO_USER_ID" ]] || { echo "ABORT: IAGO_TELEGRAM_USER_ID not set"; exit 1; }`; Phase 1 hello-world acceptance gate check: `[[ -f runtime/PHASE-1-EVIDENCE.md ]]`; `tailscale ssh root@$VPS_HOST -- 'test -d /var/lib/iago-os/daemon-state'`; `tailscale ssh root@$VPS_HOST -- 'test -f /etc/credstore.encrypted/iago-telegram-token.cred'`; `tailscale ssh root@$VPS_HOST -- 'getent passwd iago > /dev/null'`; `tailscale ssh root@$VPS_HOST -- 'test -f /etc/iago-os/santiago-age.pub'`. Require `IAGO_CUTOVER_CONFIRM=YES` (refuse to proceed otherwise). **GLOBAL LOCK (Codex P1-5 fix):** as the first step AFTER the pre-flight gate passes, acquire a VPS-side lock to prevent concurrent cutover/rollback runs. Implementation: `tailscale ssh root@$VPS_HOST -- 'LOCKFILE=/var/lock/iago-cutover.lock; exec 200>"$LOCKFILE"; if ! flock -n 200; then echo "ERROR: another cutover/rollback is running (PID $(cat $LOCKFILE.pid 2>/dev/null || echo unknown)). Aborting to prevent concurrent state corruption." >&2; exit 1; fi; echo $$ > "$LOCKFILE.pid"; trap "rm -f \"$LOCKFILE.pid\"" EXIT; <subsequent cutover steps run inside this lock context>'`. The lock is held for the full cutover wall-clock (default 60 min). Same lock acquired by rollback.sh (intentional — only one cutover OR rollback at a time). If operator needs to break a stale lock (process crashed mid-run), document the manual recovery: `tailscale ssh root@$VPS_HOST -- 'rm -f /var/lock/iago-cutover.lock /var/lock/iago-cutover.lock.pid'`. Then T-15 → T+60 sequence: (a) at T+00 run `archive-openclaw.sh` via `tailscale ssh root@$VPS_HOST -- 'bash /opt/iago-os/runtime/deploy/archive-openclaw.sh'`, verify `systemctl --user is-active openclaw-gateway` is "inactive"; (b) at T+02 echo "MANUAL STEP: run BotFather rotation per migration/02-telegram-bot-rotation.md, then press Enter" → `read -r`; (c) at T+05 run `provision-credentials.sh telegram-token` locally + verify systemd-creds decrypt round-trip; (d) at T+07 substitute SANTIAGO_USER_ID into systemd unit + copy to VPS: `sed "s/__SANTIAGO_TELEGRAM_USER_ID__/$SANTIAGO_USER_ID/" runtime/deploy/iago-os-v2-daemon.service | tailscale ssh root@$VPS_HOST -- 'cat > /etc/systemd/system/iago-os-v2-daemon.service && systemctl daemon-reload'`; `tailscale ssh root@$VPS_HOST -- 'systemctl enable --now iago-os-v2-daemon.service'`; verify `systemctl is-active iago-os-v2-daemon.service` is "active"; (e) at T+08 verify journalctl shows `daemon-start` event, IPC socket file exists at `/var/lib/iago-os/daemon-state/ipc.sock`; (f) at T+10 echo "MANUAL: send /agents to bot from phone; press y if bot replies, n to rollback" → `read -r reply` → if not y → invoke `rollback.sh`; (g) at T+15 echo canonical workflow test command from spec § 8 T+15 block (operator copy-paste); (h) at T+30 echo "MANUAL: run revoke-whatsapp.sh per migration/02-whatsapp-deauth.md with env vars exported"; (i) at T+45 + T+50 + T+55 run the spec § 8 verification-command-block (3 sanity checkpoints); (j) at T+60 echo "CUTOVER COMPLETE." + reminder list (write Obsidian digest, update STATE.md, stay-at-keyboard 30 min monitoring). Each major step writes one NDJSON line to `/var/log/iago-os/cutover.ndjson` on the VPS (via `tailscale ssh ... -- "echo '...' >> /var/log/iago-os/cutover.ndjson"` — file must be writable by root, mode 0600). The script is RESUMABLE via `IAGO_CUTOVER_RESUME_FROM=Tnn` env var — skips earlier steps and starts from the named checkpoint. File 250-450 lines.
- **verify:** `bash -n runtime/deploy/cutover.sh && shellcheck runtime/deploy/cutover.sh && grep -c "^# T+\|^echo \"T+\|^echo \"\\[T" runtime/deploy/cutover.sh`
- **expected:** `bash -n` exit 0. `shellcheck` exit 0. Sequence-marker count ≥10 (T-15, T+00, T+02, T+05, T+07, T+08, T+10, T+15, T+30, T+45, T+50, T+55, T+60).

### Task 2: Author rollback.sh

- **files:** `runtime/deploy/rollback.sh`
- **action:** Executable bash script wrapping spec § 9 ≤4-minute rollback. Shebang + `set -euo pipefail`. Header: purpose + when-to-run (any of the 6 spec § 9 detection-trigger signals) + ≤4-min wall-clock target + explicit warning that rollback is one-way (re-cutover requires fresh BotFather token). Required: `IAGO_ROLLBACK_CONFIRM=YES` env var. Optional: `IAGO_ROLLBACK_SKIP_TOKEN=1` (true if rollback happens BEFORE T+05 of cutover, meaning the OpenClaw token was never revoked). Constants reuse cutover.sh. **GLOBAL LOCK (Codex P1-5 fix):** as the first step (BEFORE T+R+0:30 stop-v2), acquire the same VPS-side lock that cutover.sh uses: `tailscale ssh root@$VPS_HOST -- 'LOCKFILE=/var/lock/iago-cutover.lock; exec 200>"$LOCKFILE"; if ! flock -n 200; then echo "ERROR: another cutover/rollback is running (PID $(cat $LOCKFILE.pid 2>/dev/null || echo unknown)). Aborting to prevent concurrent state corruption." >&2; exit 1; fi; echo $$ > "$LOCKFILE.pid"; trap "rm -f \"$LOCKFILE.pid\"" EXIT'`. Same flock pattern as cutover.sh — only one cutover OR rollback at a time (intentional; the operator MUST NOT trigger rollback while a cutover is mid-flight; they should let it complete or kill the cutover process first, then break the stale lock per the manual recovery in Task 1). Steps: (T+R+0:30) stop v2 daemon — `tailscale ssh root@$VPS_HOST -- 'systemctl stop iago-os-v2-daemon.service && systemctl disable iago-os-v2-daemon.service'`; verify `is-active = inactive`; emit NDJSON `{ kind: "rollback-step", t: "+0:30", action: "stop-v2", result: "ok" }`; (T+R+1:30) IF NOT IAGO_ROLLBACK_SKIP_TOKEN: echo "MANUAL: open BotFather, /mybots → bot → API Token → Revoke (fresh token). Paste new token here (will be passed to OpenClaw config):" → `read -r FRESH_TOKEN` (silent read via `read -rs` so token not echoed); IF IAGO_ROLLBACK_SKIP_TOKEN=1: echo "Skipping token re-rotation (OpenClaw token still valid)." (T+R+2:00) IF NOT SKIP: patch OpenClaw config via a TEMP-SCRIPT-OVER-SSH pattern (NEVER nested-quoted SSH+su+jq — see stress-test C3 + Codex P0-1). Write a small bash patch script to a local tempfile via heredoc, scp it to the VPS at `/tmp/iago-rollback-patch-<unix>.sh`, invoke via `tailscale ssh root@$VPS_HOST -- bash /tmp/iago-rollback-patch-<unix>.sh` (the script reads `FRESH_TOKEN` from env injected over SSH, not as command-line arg), then rm the remote temp. The temp script content: `set -euo pipefail; cd ~ilsantino; cp ~ilsantino/.openclaw/openclaw.json ~ilsantino/.openclaw/openclaw.json.pre-rollback; jq --arg t "$FRESH_TOKEN" '.channels.telegram.botToken = $t' ~ilsantino/.openclaw/openclaw.json > ~ilsantino/.openclaw/openclaw.json.tmp; mv ~ilsantino/.openclaw/openclaw.json.tmp ~ilsantino/.openclaw/openclaw.json; chown ilsantino:ilsantino ~ilsantino/.openclaw/openclaw.json*`. SSH the script with `tailscale ssh root@$VPS_HOST -o SendEnv=FRESH_TOKEN -- 'bash /tmp/iago-rollback-patch-<unix>.sh'` (FRESH_TOKEN forwarded via SSH SendEnv; not on argv; not visible to `ps`). Also add a fixture-based local test in test-cutover.mjs: take a fixture openclaw.json with placeholder botToken, patch, assert the output JSON equals the expected post-patch JSON byte-for-byte. The CANONICAL spec § 9 form (line 1277-1284) is for reference only — the implementation uses this safer pattern. (T+R+2:30) start OpenClaw: `tailscale ssh root@$VPS_HOST -- 'su - ilsantino -c "systemctl --user enable --now openclaw-gateway.service"'`; verify is-active. (T+R+4:00) echo "MANUAL: send /status to OpenClaw bot from phone. Press y if it replies, n for escalation." → `read -r reply` → if y → echo "ROLLBACK COMPLETE" + post-rollback action list; if n → echo "ESCALATE: capture `journalctl --user-unit openclaw-gateway --since '5 minutes ago'` and notify Sebas via Signal or phone call (Telegram path is broken)" → exit 2. Each step writes one NDJSON line to `/var/log/iago-os/cutover.ndjson` (kind: "rollback-step"). Final lines echo state-preservation reminder per spec § 9 ("do NOT delete /var/lib/iago-os/daemon-state — diagnostic"). File 150-300 lines.
- **verify:** `bash -n runtime/deploy/rollback.sh && shellcheck runtime/deploy/rollback.sh && grep -c "^# T+R\|^echo \"T+R\|^echo \"\\[T+R" runtime/deploy/rollback.sh`
- **expected:** `bash -n` exit 0. `shellcheck` exit 0. Rollback-step marker count ≥5 (+0:30, +1:30, +2:00, +2:30, +4:00).

### Task 3: Author 02-cutover-runbook.md

- **files:** `runtime/migration/02-cutover-runbook.md`
- **action:** **PATH TRANSLATION (Codex P0-2 fix):** All `bash /opt/iago-os/scripts/vps-bootstrap/<name>.sh` commands in spec § 8 translate to `bash /opt/iago-os/runtime/deploy/<name>.sh` per CONTEXT.md path-translation constraint. The runbook ships the TRANSLATED commands verbatim, NOT the spec's original commands. Specifically: spec line 1095-1096 `bash /opt/iago-os/scripts/vps-bootstrap/archive-openclaw.sh` becomes `bash /opt/iago-os/runtime/deploy/archive-openclaw.sh`; same applies to every `scripts/vps-bootstrap/` reference in spec § 8 — translate before pasting. Human-readable runbook copying spec § 8 verbatim (with the path translation applied throughout). Sections: (1) Owner — "Santiago"; (2) Wall-clock target — 60 minutes; **(3a) Day -1: Pre-cutover prep (T-24h)** — pre-merge adversarial review I6 fix; lists the exact commands to create every condition the cutover.sh pre-flight gate checks. Items, each its own `- [ ]` checkbox with the exact command + verify command: (i) Create state + log directories on VPS: `tailscale ssh root@srv1456441 -- 'mkdir -p /var/lib/iago-os/daemon-state /var/log/iago-os && chown iago:iago /var/lib/iago-os/daemon-state /var/log/iago-os && chmod 0700 /var/lib/iago-os/daemon-state'` ; verify: `tailscale ssh root@srv1456441 -- 'stat -c "%U:%G %a" /var/lib/iago-os/daemon-state'` → expect `iago:iago 700`. (ii) Create `iago` system user (skip if already exists): `if ! tailscale ssh root@srv1456441 -- 'getent passwd iago > /dev/null 2>&1'; then tailscale ssh root@srv1456441 -- 'useradd --system --no-create-home --shell /usr/sbin/nologin iago'; fi` ; verify: `tailscale ssh root@srv1456441 -- 'getent passwd iago'` → returns iago line. (iii) Upload age pubkey: `scp ~/.age/santiago.pub root@srv1456441:/etc/iago-os/santiago-age.pub && tailscale ssh root@srv1456441 -- 'chmod 0644 /etc/iago-os/santiago-age.pub'` ; verify: `tailscale ssh root@srv1456441 -- 'test -f /etc/iago-os/santiago-age.pub && wc -c /etc/iago-os/santiago-age.pub'` → returns positive byte count. (iv) Confirm 1Password items exist for ALL Phase 2 credentials: `op item list --vault iago-os | grep -E 'v2-'` → expect `v2-daemon-telegram-bot`, `v2-gh-token`, `v2-anthropic-default`, `v2-anthropic-ilsantino`, `v2-anthropic-iaguito` (5 items). If any missing, create + paste value before proceeding. (v) Provision Phase 2 active credentials onto VPS: `runtime/deploy/provision-credentials.sh telegram-token gh-token` ; verify: `tailscale ssh root@srv1456441 -- 'ls -la /etc/credstore.encrypted/'` → shows `iago-telegram-token.cred` + `iago-gh-token.cred` with `0600 root:root`. (vi) Export Santiago's Telegram user ID in the cutover shell before running cutover.sh: `export IAGO_TELEGRAM_USER_ID=<your numeric Telegram user ID>` ; verify: `echo $IAGO_TELEGRAM_USER_ID` → numeric (e.g., `123456789`); if forgotten, send any message to @userinfobot in Telegram to retrieve. Each step has its own checkbox + verify command; together they create every condition the cutover.sh pre-flight gate (Task 1) checks. **(3) Pre-cutover gate** — exact 12-checkbox list from spec § 8 with checkboxes `- [ ]` ready for Santiago to tick (these are the GATE checks; § 3a above is the PREP that creates the conditions the gate checks); (4) Cutover sequence — copy the T-15 → T+60 block from spec § 8 verbatim with formatting preserved (code blocks for verify commands, ROLLBACK TRIGGER warnings inline); (5) Verification command summary — spec § 8 final block (the `tailscale ssh ... -- 'systemctl is-active + ls agents + tail telemetry + journalctl heartbeat'` block); (6) Post-cutover required actions — Obsidian session digest path + STATE.md update + 30-min stay-at-keyboard monitoring; (7) Failure escalation — when in doubt, invoke rollback.sh (see 02-rollback-runbook.md); when rollback also fails, escalate to Sebas via Signal/phone call; (8) Reference to `cutover.sh` — "The executable in runtime/deploy/cutover.sh automates the deterministic parts. Manual steps (BotFather UI, Telegram phone testing, WhatsApp deauth click-path) remain operator-driven and are clearly marked in the script with MANUAL prompts." File 350-550 lines (was 250-450; the Day -1 prep section adds 100-150 lines).
- **verify:** `wc -l runtime/migration/02-cutover-runbook.md && grep -c "^## \|^### " runtime/migration/02-cutover-runbook.md && grep -c "T+\|T-\|T-24h" runtime/migration/02-cutover-runbook.md && grep -c "Day -1" runtime/migration/02-cutover-runbook.md`
- **expected:** Line count 350-550 (Day -1 prep section adds 100-150 lines). ≥9 section/sub-section headings. ≥18 T-time references. ≥1 "Day -1" reference.

### Task 4: Author 02-rollback-runbook.md

- **files:** `runtime/migration/02-rollback-runbook.md`
- **action:** Human-readable runbook copying spec § 9 verbatim. Sections: (1) Detection — the 6-row trigger table from spec § 9; (2) Automated watchdog snippet — spec § 9 `while true` loop; (3) Rollback steps — the T+R+0:00 → T+R+5:00 block verbatim; (4) State preservation — spec § 9 "Keep /var/lib/iago-os/daemon-state intact" justification; (5) Target wall clock table — spec § 9 5-row table; (6) Post-rollback required actions — incident note path, STATE.md update, Sebas notification, do-NOT-retry-today guidance; (7) Reference to `rollback.sh` — automation note. File 150-300 lines.
- **verify:** `wc -l runtime/migration/02-rollback-runbook.md && grep -c "T+R\|trigger" runtime/migration/02-rollback-runbook.md`
- **expected:** Line count 150-300. ≥10 T+R references + trigger keyword.

### Task 5: Author 02-cutover-decisions.md

- **files:** `runtime/migration/02-cutover-decisions.md`
- **action:** Decision log per spec § 6 final paragraph ("Phase 2 PR contains a section documenting this verdict + the 72 KiB fact about archive contents"). Sections: (1) Purpose — "Locked decisions made for Phase 2 cutover. Re-litigating these without new evidence wastes cycles. New evidence = explicit ADR overriding."; (2) LanceDB drop — copy spec § 6 verdict table (a/b/c) + the action sentence ("the 72 KiB of LanceDB data is preserved inside the encrypted OpenClaw archive; no active migration") + the "post-Phase 2 housekeeping" path; (3) User=iago system user — copy spec § 1 verdict table + provisioning command (`useradd --system ...`); (4) Telegram Option A (rotate, don't replace) — copy spec § 3 verdict table; (5) FAST cutover (not "alongside OpenClaw" parallel-run) — Santiago override 2026-05-16 paragraph; (6) No staging VPS — Santiago override of spec § 10 criterion 3 staging recommendation (rationale: spec proposed $9/mo staging KVM; Santiago opted for local dry-run via test-cutover.mjs); (7) Anthropic profiles provisioned-not-activated — spec § 5 paragraph; (8) WhatsApp deauth at cutover — Santiago decision 2026-05-13 paragraph. Each section ends with a "Reversibility" line stating what would need to be true to revisit. File 200-350 lines.
- **verify:** `wc -l runtime/migration/02-cutover-decisions.md && grep -c "^## \|^### " runtime/migration/02-cutover-decisions.md && grep -c -i "verdict\|locked\|reversibility" runtime/migration/02-cutover-decisions.md`
- **expected:** Line count 200-350. ≥8 sections. ≥10 verdict/locked/reversibility references.

### Task 6: Author test-cutover.mjs dry-run harness

- **files:** `runtime/scripts/test-cutover.mjs`
- **action:** Node ESM Vitest-style script (matches pattern of any existing `runtime/scripts/*.mjs`; if none exist, model after `runtime/integration/hello-world.test.ts`). Imports `node:child_process` `spawnSync`. Goal: dry-run BOTH `cutover.sh` and `rollback.sh` end-to-end with every external command stubbed so no real VPS is touched. Setup: create a temp dir with stub binaries `tailscale`, `systemd-creds`, `op`, `age`, `tar`, `curl`, `jq`, `sha256sum`, `shred`, `find` (each is a 5-line bash script that records its argv to a log file + returns the expected stub output — e.g., `tailscale ssh root@srv1456441 -- 'systemctl is-active iago-os-v2-daemon.service'` → stub returns "active"). Prepend temp dir to PATH. Test cases: (1) cutover.sh refuses without IAGO_CUTOVER_CONFIRM=YES; (2) cutover.sh refuses without IAGO_TELEGRAM_USER_ID set; (3) cutover.sh refuses if pre-flight check fails (stub `tailscale ssh ... -- 'test -d /var/lib/iago-os/daemon-state'` to return 1 once → script exits with the exact failing check named); (4) happy-path cutover with all stubs succeeding + `IAGO_CUTOVER_NONINTERACTIVE=1` skipping the MANUAL `read -r` prompts → exits 0; stub-invocation log shows: archive-openclaw invoked, provision-credentials invoked once with `telegram-token`, systemd daemon-reload + enable + start invoked, journalctl checked, IPC socket checked; (5) cutover with rollback trigger at T+10 (stub returns failure for the bot-reply check) → script invokes rollback.sh; (6) rollback.sh refuses without IAGO_ROLLBACK_CONFIRM=YES; (7) happy-path rollback with IAGO_ROLLBACK_SKIP_TOKEN=1 → exits 0; stub log shows systemctl stop v2 + systemctl --user start openclaw; (8) rollback without SKIP_TOKEN → requires FRESH_TOKEN via stdin (test feeds `echo "fresh-token"`) → stub log shows the jq patch invocation. Asserts via `expect(spawnResult.status).toBe(0/1/2)` + `expect(fs.readFileSync(stubLogPath, "utf8")).toMatch(/tailscale ssh.*archive-openclaw/)`. Run via `node --test runtime/scripts/test-cutover.mjs` OR adapt to Vitest if simpler — pick the path that integrates cleanly with `runtime/` existing test runner. File 250-450 lines.
- **verify:** `cd runtime && node --test scripts/test-cutover.mjs 2>&1 | tail -25  ||  npx vitest run scripts/test-cutover.mjs 2>&1 | tail -25`
- **expected:** All 8 test cases pass. Stub log files confirm correct invocation order. No real `tailscale` or `systemd-creds` binary on PATH (test PATH manipulation correctly isolates).

## Verification

```bash
for s in cutover.sh rollback.sh; do
  bash -n "runtime/deploy/$s" && shellcheck "runtime/deploy/$s" || { echo "FAIL: $s"; exit 1; }
done
wc -l runtime/migration/02-cutover-runbook.md runtime/migration/02-rollback-runbook.md runtime/migration/02-cutover-decisions.md
cd runtime && (node --test scripts/test-cutover.mjs 2>&1 | tail -30 || npx vitest run scripts/test-cutover.mjs 2>&1 | tail -30)
```

Expected:
- Both deploy scripts pass `bash -n` + `shellcheck`
- Cutover runbook 250-450 lines; rollback runbook 150-300; decisions 200-350
- test-cutover.mjs all 8 cases pass

## Stress Test

**Verdict:** PROCEED_WITH_NOTES
**Date:** 2026-05-17
**Reviewer:** orchestrator inline

### Critical (must fix in impl)

- **C1 — Cutover script's `IAGO_CUTOVER_NONINTERACTIVE=1` flag is missing from Task 1.** Task 6 test harness assumes it exists; Task 1 must add it. Without this flag, test-cutover.mjs cannot exercise happy-path because step (b) `read -r` blocks forever. **Fix:** Add `IAGO_CUTOVER_NONINTERACTIVE=1` env var support to Task 1 cutover.sh: when set, all `read -r` prompts are bypassed AND replaced with `sleep 1` (preserves the wall-clock semantics of "operator confirms by hitting Enter"). The MANUAL section names still print (so the log shows what would be done). The flag MUST log a loud warning: "DRY-RUN MODE — manual steps simulated as instant success. Real cutover MUST NOT use this flag." Reject cutover.sh runs that have BOTH `IAGO_CUTOVER_NONINTERACTIVE=1` AND `IAGO_CUTOVER_CONFIRM=YES` set together: this combination is contradictory (you're saying "this is real, but skip the human checks"). Test harness uses NONINTERACTIVE without CONFIRM=YES — the pre-flight gate exit-1-without-CONFIRM happens FIRST, so the test must mock past the gate via a third flag `IAGO_CUTOVER_DRY_RUN=1` which sets NONINTERACTIVE + treats CONFIRM as if YES. Document the flag matrix clearly.
- **C2 — Rollback.sh `read -rs FRESH_TOKEN` blocks test harness.** Same problem as C1. **Fix:** Add `IAGO_ROLLBACK_DRY_RUN=1` env var that injects a fake token `"DRYRUN_TOKEN_AAA"` and bypasses all `read` prompts. Logs same DRY-RUN warning. test-cutover.mjs uses this for test case 5 (cutover-triggers-rollback) + test case 8 (rollback-without-SKIP-token).
- **C3 — Cutover.sh quote-escaping in the jq patch SSH command.** Spec § 9 has the canonical form but bash → ssh → sh → jq → bash again has 4 quote levels. Easy to break. **Fix:** Use a heredoc pattern: write the patch script to a local temp file, `scp` it to the VPS, invoke it via `tailscale ssh ... -- bash /tmp/<unique>.sh`, then `rm` remote. Eliminates 2 quote levels. The temp script reads FRESH_TOKEN from env so it never appears on the command line (process listing). Adds 3-4 lines; pays for itself the first time someone debugs a quote-escaping issue at 2am during rollback.

### Important (forward to impl, don't block)

- **I1 — Telemetry NDJSON file `/var/log/iago-os/cutover.ndjson` creation.** Both cutover.sh and rollback.sh write here. Path must exist + be writable by root. Add to cutover.sh pre-flight: `tailscale ssh root@$VPS_HOST -- 'mkdir -p /var/log/iago-os && touch /var/log/iago-os/cutover.ndjson && chmod 0640 /var/log/iago-os/cutover.ndjson'`. Without this, the first NDJSON write silently fails (script continues, evidence is lost).
- **I2 — Resumability via `IAGO_CUTOVER_RESUME_FROM=Tnn`.** Task 1 mentions this; ensure each numbered step actually checks the resume marker before executing. Pattern: `should_run() { local stage=$1; [[ -z "$IAGO_CUTOVER_RESUME_FROM" ]] || [[ "$stage" > "$IAGO_CUTOVER_RESUME_FROM" || "$stage" == "$IAGO_CUTOVER_RESUME_FROM" ]]; }`. Each step: `should_run "T+07" && { ... }`. Test harness includes a test case for `RESUME_FROM=T+10` skipping the first 4 steps.
- **I3 — Cutover decisions doc must include the test-cutover.mjs dry-run section.** Task 5 enumerates spec-§-6 decisions. Add a section "No staging VPS — Santiago override" referencing test-cutover.mjs as the substitute and explaining why it's sufficient (every external command stubbed; tests exact invocation order + failure paths; cost = 5 min CI vs $9/mo + 1-week-of-spinup-overhead for a staging KVM). Marks the trade-off explicitly so a future reviewer doesn't second-guess.
- **I4 — Rollback wall-clock 4:00 target.** Spec § 9 table sums to 4:00. Task 2 must NOT add steps that pad this. The post-rollback action list (incident note, STATE.md update, Sebas notification) is OUTSIDE the rollback wall clock — execute after the bot smoke test succeeds (T+R+4:00+). Make this explicit in the script: comment after T+R+4:00 success: `# Wall clock target met. Post-rollback actions below are not time-critical.`
- **I5 — `cutover.sh` and `rollback.sh` should be invokable from any CWD.** Task 1 says `DEPLOY_DIR="$(dirname "$0")"` — depends on the script being invoked with a path that resolves. Use `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"` instead — robust against `./cutover.sh` vs `bash deploy/cutover.sh` vs `/opt/iago-os/runtime/deploy/cutover.sh` invocation patterns.

### Minor

- M1 — Spec § 8 T-05 sends "v2 cutover starting" via OpenClaw — confirms baseline. Optional; can be skipped by an operator confident in baseline. Task 1 script makes it optional via `IAGO_CUTOVER_SKIP_TMINUS5_BASELINE=1`.
- M2 — Decisions doc (Task 5) overlaps with spec content. Resist the urge to summarize — copy verbatim so the spec stays the canonical source and the migration doc is a self-contained reference for operators not reading the research file.

### Dimension-by-dimension verdicts

- **Precision:** Each script + runbook has line counts + grep counts + behavioral tests via the harness. The harness (Task 6) gives every assertion a concrete fixture.
- **Edge cases:** C1 + C2 + C3 cover the script-vs-test-harness interaction failures + the rollback quote-escaping hazard. I2 covers resumability when a cutover is interrupted mid-flight (e.g., Santiago's laptop dies between T+07 and T+10).
- **Contradictions:** No spec § 8 step is dropped or reordered. Cutover.sh + cutover-runbook.md must be kept in sync — Task 1 + Task 3 derive from the SAME spec § 8 text; any future spec amendment must update both. Document this coupling in Task 3 footer.
- **Simpler alternatives:** Could write cutover purely as a runbook with no executable. REJECTED — Garry standard says every checkpoint should be machine-verified where possible; running `tailscale ssh ... -- systemctl is-active` by hand vs script means script wins on consistency. Could provision a staging KVM. REJECTED per Santiago override (CONTEXT.md OQ2). Dry-run harness gives 90% of the staging-VPS confidence at 0% of the cost.
- **Missing acceptance criteria:** Spec § 10 criterion 6 (rollback path tested) → Task 6 harness exercises rollback. Criterion 3 (integration test) → harness IS the integration test (modulo no real VPS). Criterion 7 (verification path) → Plan 05 Task 3 wraps everything.

### Implementer forward-list

1. Add `IAGO_CUTOVER_DRY_RUN=1` (and `IAGO_CUTOVER_NONINTERACTIVE=1`) flag matrix to Task 1 cutover.sh (C1 fix).
2. Add `IAGO_ROLLBACK_DRY_RUN=1` (and `IAGO_ROLLBACK_SKIP_TOKEN=1`) flag matrix to Task 2 rollback.sh (C2 fix).
3. Refactor jq patch invocation to a temp-file-over-scp pattern in rollback.sh (C3 fix).
4. Add `mkdir -p /var/log/iago-os && touch + chmod` to cutover.sh pre-flight (I1 fix).
5. Implement `should_run` resume-marker pattern + add resume test case to harness (I2 fix).
6. Add the "no staging VPS — dry-run harness substitute" section to cutover-decisions.md (I3 fix).
7. Mark post-rollback actions as outside-wall-clock in rollback.sh comments (I4 fix).
8. Use `SCRIPT_DIR=$(cd ... pwd)` pattern in both scripts (I5 fix).
9. Add the `IAGO_CUTOVER_SKIP_TMINUS5_BASELINE=1` opt-out to cutover.sh (M1 fix).
