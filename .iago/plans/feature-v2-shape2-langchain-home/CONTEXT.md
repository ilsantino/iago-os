# `.iago/plans/feature-v2-shape2-langchain-home/` — v2 Shape 2 HTTP/SDK + LangChain Host (MWP L2 Stage Contract)

**Stage:** iago-os v2 Phase 3 — Shape 2 (HTTP/SDK) adapters (`anthropic-sdk`, `openai-sdk`) under `runtime/agent-runtime/http/` PLUS one runnable LangGraph host-process example agent. Realizes Santiago's explicit 'i want langchain' ask and the spec's 'LangGraph runs as an HTTP-shape agent — v2 hosts it' (vision:255, master-prompt:293-295).
**Layer:** L2 — stage contract.
**Token budget:** 200–500 tokens of contract content.
**Sibling stage (planned — CONTEXT not yet created; first step of the Phase-3 plan per `.iago/research/2026-05-30-v2-sharpened-action-plan.md`):** `feature-v2-codex-cohabitation` (the codex-pty/gemini-pty Shape-1 expansion — both land in Phase 3; together they make the video's 'Claude + Codex + LangChain side by side' literally runnable).
**Preceding stage:** `.iago/plans/feature-v2-phase-1-daemon/CONTEXT.md` (Phase 1 — AgentRuntime interface + registry + claude-pty shipped; http/ dir does NOT yet exist on disk).

This stage produces 2–4 plan files. Acceptance: an `anthropic-sdk` agent and a runnable LangGraph host agent both register in the daemon, claim file-bus tasks, and appear in Telegram /agents — proving cohabitation end-to-end, not just typed.

## Inputs

### Layer 3 (reference — internalized as constraints, NOT processed as input)
- `docs/specs/iago-os-v2-master-prompt.md` § P1 item 5 (Shape 2 adapters, 2026-05-29 LangChain-promotion amendment) + § P1 item 5 Shape 2 block (lines 293-295)
- `docs/specs/iago-os-v2-vision.md` § Agent Shape Taxonomy (Shape 2 lifecycle: generation tokens, idempotency keys, slot-based isAlive, no in-flight replay) + § Open-Q7 (LangGraph checkpointer vs session.jsonl replay — DECIDE before execution)
- `.iago/research/2026-05-29-cortextos-comms-gap-analysis.md`
- `.iago/decisions/2026-05-15-agent-shape-taxonomy.md` (5-shape interface) + § HTTP-shape auth (systemd LoadCredential=, line 490)
- `runtime/agent-runtime/registry.ts` + `types.ts` (AgentRuntime interface to implement) + `runtime/agent-runtime/pty/claude-pty.ts` (reference adapter shape)
- Context7 MCP for LangGraph + Anthropic SDK + OpenAI SDK current API
- `CLAUDE.md` (L0) + `.claude/rules/tdd.md` + `.claude/rules/aws-amplify.md` (Lambda Node 20 if any) + `.claude/rules/execution-pipeline.md`

### Layer 4 (product — processed as input each run)
- *Plan files (`NN-*.md`)* — generated by `/iago-plan --feature`, consumed by `/iago-execute feature-v2-shape2-langchain-home`

## Process
1. **Plan** — `/iago-plan --feature "Shape 2 HTTP/SDK adapters anthropic-sdk.ts + openai-sdk.ts implementing AgentRuntime (generation tokens, idempotency keys, slot-based isAlive), under runtime/agent-runtime/http/, plus one runnable LangGraph host-process example agent that registers in the daemon and claims a file-bus task" --research` (codebase scan to confirm registry contract).
2. **Stress test** — surface: Open-Q7 checkpointer decision, LoadCredential= secret wiring, idempotency-key dedupe for SDK side effects, no-in-flight-replay semantics.
3. **Approval gate** — present to Santiago; confirm Open-Q7 resolved.
4. **Execute** — `/iago-execute feature-v2-shape2-langchain-home`. Full pipeline.
5. **Verify** — anthropic-sdk agent + LangGraph host agent both alive in daemon, claim file-bus tasks, reachable via Telegram, cron-injectable; ≥80% coverage; self-evidence screenshot/log in PR.
6. **Session digest** — Obsidian; update STATE.md.

## Outputs
| Output | Location | Status |
|---|---|---|
| Shape-2 plan stack | `.iago/plans/feature-v2-shape2-langchain-home/NN-*.md` | Pending |
| `runtime/agent-runtime/http/anthropic-sdk.ts` + `openai-sdk.ts` | `runtime/agent-runtime/http/` (new dir) | Pending |
| Runnable LangGraph host-process example agent | `runtime/agents/<example>/` | Pending |
| Tests + README + telemetry | colocated | Pending |
| PR(s) @claude tagged | GitHub | Pending |

## Decided constraints (do not relitigate during planning)
- **Shape 2 is HTTP/SDK** — `anthropic-sdk` + `openai-sdk` implement the AgentRuntime interface; LangGraph runs ON TOP as the workflow layer. v2 hosts, does not reimplement.
- **systemd `LoadCredential=` for API keys** (decision locked 2026-05-15). 1Password provisioning-only.
- **Idempotency keys per side-effect** (vision Shape-4/5 replay rule applies to SDK external mutations).
- **Open-Q7 must be decided** (LangGraph checkpointer vs session.jsonl replay) before execution.
- **No Postgres, no Docker. Pipeline preserved.**
- **Garry-impressed completeness** — a runnable example, not a stub.

## Open questions to capture (not block)
- Open-Q7 checkpointer integration (decide at plan time).
- Whether the example LangGraph host uses Anthropic or OpenAI as the model backend (recommend Anthropic for parity with claude-pty).

## Source
- L0: `CLAUDE.md` · L1: `.iago/CONTEXT.md` · Sibling L2 (planned, not yet created — Phase 3): `feature-v2-codex-cohabitation/CONTEXT.md` · Decision artifact: `.iago/research/2026-05-29-cortextos-comms-gap-analysis.md`