# `.iago/plans/feature-v2-supervisor-role/` — v2 Supervisor (Chief) Agent Role (MWP L2 Stage Contract)

**Stage:** iago-os v2 Phase 3 — chief/orchestrator tier as a role flag + daemon-gated top-down task dispatch. NOT a new AgentRuntime shape, NOT agent-to-agent messaging, NOT Paperclip org-chart titles.
**Layer:** L2 — stage contract.
**Token budget:** 200–500 tokens of contract content.
**Sibling stage:** `feature-v2-agent-comms-channel/CONTEXT.md` (shares the producer-capability gate; coordinate so the gate is defined once).
**Preceding stage:** `.iago/plans/feature-v2-phase-1-daemon/CONTEXT.md` (Phase 1 — spawnSubagent + cost-rollup + cascade-shutdown shipped) and the Plan 07b task-resolved event mechanism.

This stage produces 1–3 plan files realizing master-prompt Mission #5 ('the supervisor agent dispatches; specialists claim and execute; results merge'). It does NOT reverse a lock — it implements latent spec. The 30% missing is the role declaration + supervisor's ability to enqueue worker tasks + hierarchy observability; the 70% (parent-child handles, cost rollup, cascade shutdown, mutex-guarded spawn) is already shipped.

## Inputs

### Layer 3 (reference — internalized as constraints, NOT processed as input)
- `docs/specs/iago-os-v2-master-prompt.md` § Mission #5 (line 62, 2026-05-29 chief-role amendment) + P1 item 5a
- `docs/specs/iago-os-v2-vision.md` § Agent Shape Taxonomy (Supervisor/chief subsection) + line 74 (Paperclip org-chart rejection — chief is mechanical, not titled)
- `.iago/research/2026-05-29-cortextos-comms-gap-analysis.md`
- `.iago/decisions/2026-05-15-agent-shape-taxonomy.md` (subagent-spawn adoption + 2026-05-29 addendum)
- `runtime/daemon/agent-manager.ts` (spawnSubagent:529, parentChildren linkage, getCostSummary:605 — REUSE)
- `runtime/daemon/cron-scheduler.ts` (fire() producer pattern) + `runtime/daemon/file-bus.ts` (O_EXCL + validateAgentId)
- `CLAUDE.md` (L0) + `.claude/rules/tdd.md` + `.claude/rules/execution-pipeline.md`

### Layer 4 (product — processed as input each run)
- *Plan files (`NN-*.md`)* — generated by `/iago-plan --feature`, consumed by `/iago-execute feature-v2-supervisor-role`

## Process
1. **Plan** — `/iago-plan --feature "Supervisor/chief agent role: role field in agent config, daemon-gated enqueueTask(from,to,prompt) with supervisor-only guard + same-org validation, originatingSupervisor stamp, result-merge injection back to supervisor on task-resolved, cost-rollup display, Telegram /agents shows role"` with all L3 loaded.
2. **Stress test** — surface: non-chief enqueue rejection, cross-org dispatch rejection, A→B→A loop guard, fairness/starvation (Open-Q11), Plan 07b task-resolved dependency.
3. **Approval gate** — present to Santiago (bundled with the comms decision; both touch the producer-capability gate).
4. **Execute** — `/iago-execute feature-v2-supervisor-role`. Full pipeline.
5. **Verify** — supervisor enqueues; worker cannot; cross-org rejected; result injection fires on resolve; cost rollup reflects dispatched-tree spend; demo: a claude supervisor dispatches a codex worker end-to-end (assign→claim→resolve→summary back).
6. **Session digest** — Obsidian; update STATE.md.

## Outputs
| Output | Location | Status |
|---|---|---|
| Supervisor-role plan stack | `.iago/plans/feature-v2-supervisor-role/NN-*.md` | Pending |
| `role` field + enqueueTask guard | agent-config schema + `runtime/daemon/*` | Pending |
| Result-merge injection | extend Plan 07b task-resolved event | Pending |
| Tests + README + telemetry | colocated | Pending |
| PR(s) @claude tagged | GitHub | Pending |

## Decided constraints (do not relitigate during planning)
- **Chief is a ROLE, not a shape.** `role:"supervisor"|"worker"` (default worker). Any shape can be a supervisor.
- **Top-down dispatch only.** Daemon-gated enqueueTask, supervisor-role guard, same-org validation. NO symmetric peer messaging.
- **Reuse spawnSubagent for tight coupling, enqueueTask for loose coupling.** Do not reinvent parent-child linkage or cost rollup.
- **Results merge as structured task-resolved injections** via AgentRuntime.send() — NOT free-form chat.
- **No org-chart titles** (Paperclip rejected). Mechanical dispatch permission only.
- **No Postgres, no Docker, no broker. Pipeline preserved.**
- **Garry-impressed completeness.**

## Open questions to capture (not block)
- Per-supervisor `max_concurrent_children` (default 3) as the fairness lever.
- Whether result-merge needs Plan 07b shipped first or carries it as a sub-task.

## Source
- L0: `CLAUDE.md` · L1: `.iago/CONTEXT.md` · Decision artifact: `.iago/research/2026-05-29-cortextos-comms-gap-analysis.md` · Shares producer-gate with `feature-v2-agent-comms-channel`