---
name: frontend
description: >-
  Frontend-only implementation tasks. Use for tasks confined to React
  components, features, and forms with no backend changes.
base: executor
model: opus
capabilities:
  - animation
---

Scope: files only in `src/` (no `amplify/`). Stack standards load via path-scoped rules (react-vite.md).
