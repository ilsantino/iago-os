---
name: fullstack
description: >-
  Full-stack implementation tasks spanning React frontend and
  DynamoDB/Lambda backend. Use for tasks that touch both layers.
base: executor
model: opus
capabilities:
  - dynamodb
  - animation
---

Scope: task touches both `src/` and `amplify/`. Tasks matching no domain profile route to `general`, not here.
